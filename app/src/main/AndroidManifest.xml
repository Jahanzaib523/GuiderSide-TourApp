<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.semester_project.smd_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="Safar Guider"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ForgotPasswordRecovery"></activity>
        <activity android:name=".Activities.Create_UserProfile" />
        <activity android:name=".Activities.PasswordRecovery" />
        <activity android:name=".Activities.UserVerification" />
        <activity android:name=".Activities.SignUp" />
        <activity android:name=".Activities.SignIn" />
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Navigation" />
        <activity android:name=".Activities.HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>